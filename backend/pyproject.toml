[project]
name = "phonetic-sign-backend"
version = "0.1.0"
description = "Phonetic sign language RAG translation API"
readme = "../README.md"
requires-python = ">=3.11"
license = "MIT"

[project.optional-dependencies]
dev = ["ruff>=0.8.0"]
